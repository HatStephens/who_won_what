<p>Here is a list of the Groups you currently belong to:</p>
<ul>
  <% @groups.each do |group| %>
   <li><%= group.name %> - <%= link_to("Details", group_path(group))%> <%= link_to("Stats", group_matches_path(group))%></li>
  <% end %>
</ul>
<%= link_to('Add a new Group', new_group_path) %>